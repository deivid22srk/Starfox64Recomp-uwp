diff --git a/CMakeLists.txt b/CMakeLists.txt
index 17debe8..700fdb9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -55,10 +55,13 @@ elseif (APPLE)
         set (SPIRVCROSS "DYLD_LIBRARY_PATH=${PROJECT_SOURCE_DIR}/src/contrib/spirv-cross/lib/arm64:$ENV{DYLD_LIBRARY_PATH}" "${PROJECT_SOURCE_DIR}/src/contrib/spirv-cross/bin/arm64/spirv-cross")
     endif()
 else()
-    if (CMAKE_SYSTEM_PROCESSOR STREQUAL "x86_64")
-        set (DXC "LD_LIBRARY_PATH=${PROJECT_SOURCE_DIR}/src/contrib/dxc/lib/x64" "${PROJECT_SOURCE_DIR}/src/contrib/dxc/bin/x64/dxc-linux")
+    execute_process(COMMAND uname -m OUTPUT_VARIABLE HOST_ARCH OUTPUT_STRIP_TRAILING_WHITESPACE)
+    if (HOST_ARCH STREQUAL "x86_64" OR HOST_ARCH STREQUAL "amd64")
+        set (DXC "${PROJECT_SOURCE_DIR}/src/contrib/dxc/bin/x64/dxc-linux")
+        set (DXC_ENV "LD_LIBRARY_PATH=${PROJECT_SOURCE_DIR}/src/contrib/dxc/lib/x64")
     else()
-        set (DXC "LD_LIBRARY_PATH=${PROJECT_SOURCE_DIR}/src/contrib/dxc/lib/arm64" "${PROJECT_SOURCE_DIR}/src/contrib/dxc/bin/arm64/dxc-linux")
+        set (DXC "${PROJECT_SOURCE_DIR}/src/contrib/dxc/bin/arm64/dxc-linux")
+        set (DXC_ENV "LD_LIBRARY_PATH=${PROJECT_SOURCE_DIR}/src/contrib/dxc/lib/arm64")
     endif()
 endif()
 
@@ -68,10 +71,12 @@ set(ZSTD_BUILD_SHARED OFF)
 
 add_subdirectory(src/tools/file_to_c)
 add_subdirectory(src/contrib/re-spirv)
-add_subdirectory(src/contrib/nativefiledialog-extended)
+if (NOT ANDROID)
+    add_subdirectory(src/contrib/nativefiledialog-extended)
+endif()
 add_subdirectory(src/contrib/zstd/build/cmake)
 
-if (CMAKE_SYSTEM_NAME MATCHES "Linux" AND RT64_SDL_WINDOW_VULKAN)
+if ((CMAKE_SYSTEM_NAME MATCHES "Linux" OR ANDROID) AND RT64_SDL_WINDOW_VULKAN)
     set(PLUME_SDL_VULKAN_ENABLED TRUE)
 endif()
 
@@ -109,9 +114,15 @@ set (DXC_GS_OPTS "${DXC_COMMON_OPTS}" "-E" "GSMain" "-T gs_6_3")
 set (DXC_RT_OPTS "${DXC_COMMON_OPTS}" "-D" "RT_SHADER" "-T" "lib_6_3" "-fspv-target-env=vulkan1.1spirv1.4" "-fspv-extension=SPV_KHR_ray_tracing" "-fspv-extension=SPV_EXT_descriptor_indexing")
 
 function(build_shader_spirv_impl TARGETOBJ FILENAME TARGET_NAME OUTNAME)
-    add_custom_command(OUTPUT ${OUTNAME}.spv
-        COMMAND ${DXC} ${DXC_SPV_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.spv
-        DEPENDS ${FILENAME})
+    if (DXC_ENV)
+        add_custom_command(OUTPUT ${OUTNAME}.spv
+            COMMAND ${CMAKE_COMMAND} -E env ${DXC_ENV} ${DXC} ${DXC_SPV_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.spv
+            DEPENDS ${FILENAME})
+    else()
+        add_custom_command(OUTPUT ${OUTNAME}.spv
+            COMMAND ${DXC} ${DXC_SPV_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.spv
+            DEPENDS ${FILENAME})
+    endif()
     add_custom_command(OUTPUT ${OUTNAME}.spirv.c
         COMMAND file_to_c ${OUTNAME}.spv ${TARGET_NAME}BlobSPIRV ${OUTNAME}.spirv.c ${OUTNAME}.spirv.h
         DEPENDS ${OUTNAME}.spv file_to_c
@@ -120,9 +131,15 @@ function(build_shader_spirv_impl TARGETOBJ FILENAME TARGET_NAME OUTNAME)
 endfunction()
 
 function(build_shader_dxil_impl TARGETOBJ FILENAME TARGET_NAME OUTNAME)
-    add_custom_command(OUTPUT ${OUTNAME}.dxil
-        COMMAND ${DXC} ${DXC_DXIL_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.dxil
-        DEPENDS ${FILENAME})
+    if (DXC_ENV)
+        add_custom_command(OUTPUT ${OUTNAME}.dxil
+            COMMAND ${CMAKE_COMMAND} -E env ${DXC_ENV} ${DXC} ${DXC_DXIL_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.dxil
+            DEPENDS ${FILENAME})
+    else()
+        add_custom_command(OUTPUT ${OUTNAME}.dxil
+            COMMAND ${DXC} ${DXC_DXIL_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.dxil
+            DEPENDS ${FILENAME})
+    endif()
     add_custom_command(OUTPUT ${OUTNAME}.dxil.c
         COMMAND file_to_c ${OUTNAME}.dxil ${TARGET_NAME}BlobDXIL ${OUTNAME}.dxil.c ${OUTNAME}.dxil.h
         DEPENDS ${OUTNAME}.dxil file_to_c
@@ -131,9 +148,15 @@ function(build_shader_dxil_impl TARGETOBJ FILENAME TARGET_NAME OUTNAME)
 endfunction()
 
 function(build_shader_msl_impl TARGETOBJ FILENAME TARGET_NAME OUTNAME)
-    add_custom_command(OUTPUT ${OUTNAME}.spv
-            COMMAND ${DXC} ${DXC_SPV_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.spv
-            DEPENDS ${FILENAME})
+    if (DXC_ENV)
+        add_custom_command(OUTPUT ${OUTNAME}.spv
+                COMMAND ${CMAKE_COMMAND} -E env ${DXC_ENV} ${DXC} ${DXC_SPV_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.spv
+                DEPENDS ${FILENAME})
+    else()
+        add_custom_command(OUTPUT ${OUTNAME}.spv
+                COMMAND ${DXC} ${DXC_SPV_OPTS} ${ARGN} ${FILENAME} /Fo ${OUTNAME}.spv
+                DEPENDS ${FILENAME})
+    endif()
     add_custom_command(OUTPUT ${OUTNAME}.metal
             COMMAND spirv_cross_msl ${OUTNAME}.spv ${OUTNAME}.metal
             DEPENDS ${OUTNAME}.spv spirv_cross_msl)
@@ -284,6 +307,9 @@ if (WIN32)
     set(SDL2_INCLUDE_DIRS "${PROJECT_SOURCE_DIR}/src/contrib/mupen64plus-win32-deps/SDL2-2.26.3/include")
     set(SDL2_LIBRARIES "SDL2" "SDL2main")
     link_directories("${PROJECT_SOURCE_DIR}/src/contrib/mupen64plus-win32-deps/SDL2-2.26.3/lib/x64")
+elseif (ANDROID)
+    set(SDL2_INCLUDE_DIRS "${CMAKE_SOURCE_DIR}/lib/SDL2/include")
+    set(SDL2_LIBRARIES SDL2::SDL2)
 else()
     find_package(SDL2 REQUIRED)
 endif()
@@ -296,7 +322,7 @@ add_compile_definitions(
     IMGUI_IMPL_VULKAN_NO_PROTOTYPES
 )
 
-if (CMAKE_SYSTEM_NAME MATCHES "Linux" AND RT64_SDL_WINDOW_VULKAN)
+if ((CMAKE_SYSTEM_NAME MATCHES "Linux" OR ANDROID) AND RT64_SDL_WINDOW_VULKAN)
     add_compile_definitions("PLUME_SDL_VULKAN_ENABLED")
     add_compile_definitions("RT64_SDL_WINDOW_VULKAN")
 endif()
@@ -418,9 +444,12 @@ include_directories(
     "${PROJECT_SOURCE_DIR}/src/contrib/imgui"
     "${PROJECT_SOURCE_DIR}/src/contrib/hlslpp/include"
     "${PROJECT_SOURCE_DIR}/src/contrib/mupen64plus-core/src/api"
-    "${PROJECT_SOURCE_DIR}/src/contrib/nativefiledialog-extended/src/include"
 )
 
+if (NOT ANDROID)
+    include_directories("${PROJECT_SOURCE_DIR}/src/contrib/nativefiledialog-extended/src/include")
+endif()
+
 if (WIN32)
     # DXC only needs to be included during runtime for Windows.
     include_directories("${PROJECT_SOURCE_DIR}/src/contrib/dxc/inc")
@@ -437,7 +466,9 @@ set_target_properties(rt64 PROPERTIES PREFIX "")
 
 # Add common libraries.
 target_link_libraries(rt64 re-spirv)
-target_link_libraries(rt64 nfd)
+if (NOT ANDROID)
+    target_link_libraries(rt64 nfd)
+endif()
 target_link_libraries(rt64 libzstd_static)
 target_link_libraries(rt64 plume)
 
diff --git a/src/common/rt64_filesystem_zip.cpp b/src/common/rt64_filesystem_zip.cpp
index ff6b9d6..7fe4cbe 100644
--- a/src/common/rt64_filesystem_zip.cpp
+++ b/src/common/rt64_filesystem_zip.cpp
@@ -140,7 +140,7 @@ namespace RT64 {
         }
 
 // DLW: Modded to utilize file handle and ReadFile, large memory mapped files are a no-go on UWP
-#if 0
+#if !defined(_WIN32)
         // Validate the local dir header.
         const char *localDirHeader = reinterpret_cast<const char *>(&impl->zipMappedFile.data()[it->second.localHeaderOffset]);
         if (MZ_READ_LE32(localDirHeader) != MZ_ZIP_LOCAL_DIR_HEADER_SIG) {
@@ -169,7 +169,7 @@ namespace RT64 {
         size_t dataAddress = it->second.localHeaderOffset + MZ_ZIP_LOCAL_DIR_HEADER_SIZE + ldhFilenameLenOfs + ldhExtraLenOfs;
 
         // Make sure output vector has enough bytes to hold the data.
-#if 0
+#if !defined(_WIN32)
         const uint8_t *zipFileData = reinterpret_cast<const uint8_t *>(&impl->zipMappedFile.data()[dataAddress]);
 #else
         // Move to the actual file data to decompress.
@@ -186,13 +186,17 @@ namespace RT64 {
 #endif
         if (it->second.compression == FileSystemZipInfo::Compression::Zstd) {
             if (ZSTD_decompress(fileData, it->second.uncompressedSize, zipFileData, it->second.compressedSize) != it->second.uncompressedSize) {
+#if defined(_WIN32)
                 delete[] zipFileData;
+#endif
                 return false;
             }
         }
         else if (it->second.compression == FileSystemZipInfo::Compression::Deflate) {
             if (tinfl_decompress_mem_to_mem(fileData, it->second.uncompressedSize, zipFileData, it->second.compressedSize, TINFL_FLAG_USING_NON_WRAPPING_OUTPUT_BUF) == TINFL_DECOMPRESS_MEM_TO_MEM_FAILED) {
+#if defined(_WIN32)
                 delete[] zipFileData;
+#endif
                 return false;
             }
         }
@@ -200,7 +204,9 @@ namespace RT64 {
             memcpy(fileData, zipFileData, it->second.uncompressedSize);
         }
 
+#if defined(_WIN32)
         delete[] zipFileData;
+#endif
         return true;
     }
 
diff --git a/src/common/rt64_mapped_file.cpp b/src/common/rt64_mapped_file.cpp
index 2597101..86f2a70 100644
--- a/src/common/rt64_mapped_file.cpp
+++ b/src/common/rt64_mapped_file.cpp
@@ -123,9 +123,11 @@ namespace RT64 {
 #   endif
     }
 
+#if defined(_WIN32)
     HANDLE MappedFile::handle() const {
         return fileHandle;
     }
+#endif
 
     uint8_t *MappedFile::data() const {
         return reinterpret_cast<uint8_t *>(fileView);
diff --git a/src/common/rt64_mapped_file.h b/src/common/rt64_mapped_file.h
index b121bd7..00def18 100644
--- a/src/common/rt64_mapped_file.h
+++ b/src/common/rt64_mapped_file.h
@@ -30,7 +30,9 @@ namespace RT64 {
         ~MappedFile();
         bool open(const std::filesystem::path &path);
         bool isOpen() const;
+#if defined(_WIN32)
         HANDLE handle() const;
+#endif
         uint8_t *data() const;
         size_t size() const;
     };
diff --git a/src/gui/rt64_file_dialog.cpp b/src/gui/rt64_file_dialog.cpp
index 72faede..e67470f 100644
--- a/src/gui/rt64_file_dialog.cpp
+++ b/src/gui/rt64_file_dialog.cpp
@@ -6,7 +6,9 @@
 
 #include <cassert>
 
+#if !defined(__ANDROID__)
 #include <nfd.h>
+#endif
 
 namespace RT64 {
     // FileDialog
@@ -14,13 +16,18 @@ namespace RT64 {
     std::atomic<bool> FileDialog::isOpen = false;
 
     void FileDialog::initialize() {
+#if !defined(__ANDROID__)
         NFD_Init();
+#endif
     }
 
     void FileDialog::finish() {
+#if !defined(__ANDROID__)
         NFD_Quit();
+#endif
     }
 
+#if !defined(__ANDROID__)
     static std::vector<nfdnfilteritem_t> convertFilters(const std::vector<FileFilter> &filters) {
         std::vector<nfdnfilteritem_t> nfdFilters;
         for (const FileFilter &filter : filters) {
@@ -29,8 +36,10 @@ namespace RT64 {
 
         return nfdFilters;
     }
+#endif
 
     std::filesystem::path FileDialog::getDirectoryPath() {
+#if !defined(__ANDROID__)
         isOpen = true;
 
         std::filesystem::path path;
@@ -43,9 +52,13 @@ namespace RT64 {
 
         isOpen = false;
         return path;
+#else
+        return {};
+#endif
     }
 
     std::filesystem::path FileDialog::getOpenFilename(const std::vector<FileFilter> &filters) {
+#if !defined(__ANDROID__)
         isOpen = true;
         
         std::filesystem::path path;
@@ -59,9 +72,13 @@ namespace RT64 {
 
         isOpen = false;
         return path;
+#else
+        return {};
+#endif
     }
 
     std::filesystem::path FileDialog::getSaveFilename(const std::vector<FileFilter> &filters) {
+#if !defined(__ANDROID__)
         isOpen = true;
 
         std::filesystem::path path;
@@ -75,5 +92,8 @@ namespace RT64 {
 
         isOpen = false;
         return path;
+#else
+        return {};
+#endif
     }
 };
\ No newline at end of file
diff --git a/src/hle/rt64_application.cpp b/src/hle/rt64_application.cpp
index ff103d9..a8cf709 100644
--- a/src/hle/rt64_application.cpp
+++ b/src/hle/rt64_application.cpp
@@ -275,7 +275,13 @@ namespace RT64 {
         // where the wait() will take far more time than expected and cause stuttering. While this seems to work fine everywhere else,
         // we stick to the pre-plume approach to be safe.
         const uint32_t maxFrameLatency = (chosenGraphicsAPI == UserConfiguration::GraphicsAPI::Vulkan) ? 2 : 1;
-        swapChain = presentGraphicsWorker->commandQueue->createSwapChain(appWindow->windowHandle, bufferCount, RenderFormat::B8G8R8A8_UNORM, maxFrameLatency);
+#if defined(__ANDROID__)
+        // Android swapchains are backed by AHardwareBuffer, which does not support the B8G8R8A8 format.
+        const RenderFormat swapChainFormat = RenderFormat::R8G8B8A8_UNORM;
+#else
+        const RenderFormat swapChainFormat = RenderFormat::B8G8R8A8_UNORM;
+#endif
+        swapChain = presentGraphicsWorker->commandQueue->createSwapChain(appWindow->windowHandle, bufferCount, swapChainFormat, maxFrameLatency);
 
         // Before configuring multisampling, make sure the device actually supports it for the formats we'll use. If it doesn't, turn off antialiasing in the configuration.
         const RenderSampleCounts colorSampleCounts = device->getSampleCountsSupported(RenderTarget::colorBufferFormat(usesHDR));
diff --git a/src/hle/rt64_application_window.cpp b/src/hle/rt64_application_window.cpp
index 1597bab..50aeb03 100755
--- a/src/hle/rt64_application_window.cpp
+++ b/src/hle/rt64_application_window.cpp
@@ -11,7 +11,7 @@
 #if defined(_WIN32)
 #   include <Windows.h>
 #   include <ShellScalingAPI.h>
-#elif defined(__linux__)
+#elif defined(__linux__) && !defined(__ANDROID__)
 #   define Status int
 #   if !defined(RT64_SDL_WINDOW_VULKAN)
 #      include <X11/extensions/Xrandr.h>
@@ -23,7 +23,11 @@
 
 #include "common/rt64_common.h"
 
+#if defined(_WIN32)
 extern "C" __declspec(dllimport) float uwp_GetRefreshRate();
+#else
+extern "C" float uwp_GetRefreshRate();
+#endif
 
 namespace RT64 {
     // ApplicationWindow
@@ -106,7 +110,10 @@ namespace RT64 {
         bounds.width = rect.right - rect.left;
         bounds.height = rect.bottom - rect.top;
 #   elif defined(__ANDROID__)
-        static_assert(false && "Android unimplemented");
+        bounds.left = 0;
+        bounds.top = 0;
+        bounds.width = Width;
+        bounds.height = Height;
 #   elif defined(__linux__) || defined(__APPLE__)
         if (SDL_VideoInit(nullptr) != 0) {
             printf("Failed to init SDL2 video: %s\n", SDL_GetError());
@@ -150,7 +157,7 @@ namespace RT64 {
 #   elif defined(RT64_SDL_WINDOW_VULKAN)
         windowHandle = sdlWindow;
 #   elif defined(__ANDROID__)
-        static_assert(false && "Android unimplemented");
+        windowHandle = wmInfo.info.android.window;
 #   elif defined(__linux__)
         windowHandle.display = wmInfo.info.x11.display;
         windowHandle.window = wmInfo.info.x11.window;
@@ -288,6 +295,8 @@ namespace RT64 {
         }
 
         refreshRate = displayMode.refresh_rate;
+#   elif defined(__ANDROID__)
+        refreshRate = 60;
 #   elif defined(__linux__)
         // Sourced from: https://stackoverflow.com/a/66865623
         XRRScreenResources *screenResources = XRRGetScreenResources(windowHandle.display, windowHandle.window);
@@ -343,6 +352,9 @@ namespace RT64 {
         newWindowTop = rect.top;
 #   elif defined(RT64_SDL_WINDOW_VULKAN)
         SDL_GetWindowPosition(windowHandle, &newWindowLeft, &newWindowTop);
+#   elif defined(__ANDROID__)
+        newWindowLeft = 0;
+        newWindowTop = 0;
 #   elif defined(__linux__)
         XWindowAttributes attributes;
         XGetWindowAttributes(windowHandle.display, windowHandle.window, &attributes);
diff --git a/src/tools/file_to_c/CMakeLists.txt b/src/tools/file_to_c/CMakeLists.txt
index 22ee3eb..1068059 100644
--- a/src/tools/file_to_c/CMakeLists.txt
+++ b/src/tools/file_to_c/CMakeLists.txt
@@ -2,9 +2,23 @@ cmake_minimum_required(VERSION 3.20)
 project(file_to_c)
 set(CMAKE_CXX_STANDARD 17)
 
-add_executable(file_to_c "file_to_c.cpp")
+if (CMAKE_CROSSCOMPILING)
+    set(FILE_TO_C_HOST_BINARY "${CMAKE_BINARY_DIR}/file_to_c_host")
+    add_custom_command(
+        OUTPUT ${FILE_TO_C_HOST_BINARY}
+        COMMAND g++ -std=c++17 -o ${FILE_TO_C_HOST_BINARY} ${CMAKE_CURRENT_SOURCE_DIR}/file_to_c.cpp
+        DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/file_to_c.cpp
+        COMMENT "Building file_to_c for host"
+    )
+    add_custom_target(file_to_c_build DEPENDS ${FILE_TO_C_HOST_BINARY})
+    add_executable(file_to_c IMPORTED GLOBAL)
+    set_target_properties(file_to_c PROPERTIES IMPORTED_LOCATION "${FILE_TO_C_HOST_BINARY}")
+    add_dependencies(file_to_c file_to_c_build)
+else()
+    add_executable(file_to_c "file_to_c.cpp")
+endif()
 
-if (APPLE)
+if (APPLE AND NOT CMAKE_CROSSCOMPILING)
     set_target_properties(file_to_c PROPERTIES
         XCODE_ATTRIBUTE_CODE_SIGN_IDENTITY "-"
         XCODE_ATTRIBUTE_CODE_SIGN_ENTITLEMENTS ${CMAKE_CURRENT_SOURCE_DIR}/../../../rt64.entitlements
